{
  "name": "frutigeraerostore",
  "version": "1.0.0",
  "description": "FrutigerAeroStore - Aqua Mouse landing page",
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
